import numpy as np
from util.graph import Graph

print('a')
u = np.array([2, -7])
print(u / np.linalg.norm(u))

print('b')
u = np.array([-9, 3, 7])
print(u / np.linalg.norm(u))

print('c')
u = np.array([-3, 5, 8, 6])
print(u / np.linalg.norm(u))

print('d')
u = np.array([-6, 2, 8, 3, 5])
print(u / np.linalg.norm(u))
